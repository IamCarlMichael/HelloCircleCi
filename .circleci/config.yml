version: 2
jobs:
  test:
    docker:
      - image: node:12.13.0-alpine
    steps:
      - checkout
      - run:
          name: "Package Manager installation"
          command: "npm install"
      - run:
          name: "Run test"
          command: "npm test"
      - run:
          name: "echo hello"
          command: "echo hello"

workflows:
  version: 2
  runTest:
    jobs:
      - test
